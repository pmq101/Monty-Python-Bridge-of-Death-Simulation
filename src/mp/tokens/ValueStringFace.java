package mp.tokens;
import util.annotations.Tags;

@Tags ({"Word", "Move", "Say", "Repeat", "Approach", "Pass", "Fail"})

public interface ValueStringFace extends Tokens {

	public String getValue();
}
